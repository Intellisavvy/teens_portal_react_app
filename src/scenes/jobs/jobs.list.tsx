import { JobCard } from "./jobCard"


export const JobsList = () => {
    return(
        <JobCard/>
    )
    // const jobs = [1,2,3,4,5]
    // return <div>{jobs.map(sim => {
    //     return <JobCard />
    // })}</div>
}